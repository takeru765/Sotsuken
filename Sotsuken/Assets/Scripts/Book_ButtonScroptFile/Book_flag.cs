using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.EventSystems;

public class Book_flag : MonoBehaviour
{
   /* public Counter_test TrashPoint;//現在は仮のスクリプト。Gamemanegerから個別で登録した個数を引用する
    public Sprite trashSprite;
    private Image image;
    public GameObject Button_Object;

    int Btrash_alumi;//引用した数値を入れる変数
    int Btrash_steal;
    int Btrash_pet;
    int Btrash_pla;
    int Btrash_paper;

    void Start()
    {
        Button btn = GetComponent<Button>();
        btn.interactable = false;//ボタン機能を無効にする

        image = GetComponent<Image>();

    }



    // Update is called once per frame
    void Update()
    {
        //Btrash = TrashPoint.trash;
        Btrash_steal = TrashPoint.steal;
        Btrash_alumi = TrashPoint.alumi;//引用した数値を入れる変数
        Btrash_pet   = TrashPoint.pet;
        Btrash_pla  = TrashPoint.pla;
        Btrash_paper = TrashPoint.paper;

        if (Btrash_alumi == 1)//仮の数値。登録したごみが１個以上になった場合に、図鑑の項目が出現するようになる。
        {
            Button btn = Button_Object.GetComponent<Button>();//インスペクターのボタンオブジェクトで指定したボタンの解放、このままではほかのオブジェクトも同時に処理してしまうので、もっと細かい指定が必要。
            btn.interactable = true;

            image.sprite = trashSprite;//画像を変える
        }
        if (Btrash_steal == 2)//仮の数値。登録したごみが１個以上になった場合に、図鑑の項目が出現するようになる。
        {
            Button btn = Button_Object.GetComponent<Button>();
            btn.interactable = true;

            image.sprite = trashSprite;//画像を変える
        }
        if (Btrash_pet == 3)//仮の数値。登録したごみが１個以上になった場合に、図鑑の項目が出現するようになる。
        {
            Button btn = Button_Object.GetComponent<Button>();
            btn.interactable = true;

            image.sprite = trashSprite;//画像を変える
        }
        if (Btrash_pla == 4)//仮の数値。登録したごみが１個以上になった場合に、図鑑の項目が出現するようになる。
        {
            Button btn = Button_Object.GetComponent<Button>();
            btn.interactable = true;

            image.sprite = trashSprite;//画像を変える
        }
        if (Btrash_paper == 5)//仮の数値。登録したごみが１個以上になった場合に、図鑑の項目が出現するようになる。
        {
            Button btn = Button_Object.GetComponent<Button>();
            btn.interactable = true;

            image.sprite = trashSprite;//画像を変える
        }

    }*/
}